// tv/epg.js — EPG of channel rows × horizontal time. (Stub in Task 2; the rows
// renderer + filter land in Task 3.)
(function () {
  "use strict";
  function render() {}
  function filter() {}
  function refilter() {}
  if (typeof window !== "undefined") window.EPG = { render, filter, refilter };
})();
